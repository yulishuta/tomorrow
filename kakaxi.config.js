module.exports = {
  // 打包成 umd 导出的全局名称
  // globalName: '',
  // 是否上传 cdn，默认是 false
  // cdn: false,
  // 是否打包 es 模块导出的文件，默认为 true
  esModule: false,
  // 公共包运行的环境，可以选 node 和 browser，默认是 browser
  env: 'node'
};
